#!/bin/sh
set -e

case "$1" in
    configure)
        if command -v setfacl >/dev/null 2>&1; then
            setfacl -m d:u:etvpnsqlwww:r /var/lib/etvpn/port-share 2>/dev/null || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
